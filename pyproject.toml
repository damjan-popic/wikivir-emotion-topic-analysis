[project]
name = "wikivir-emotion-topic-analysis"
version = "0.1.0"
description = "Emotion and topic analysis for CLASSLA-annotated Wikivir corpora"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
